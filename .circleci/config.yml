version: 2

jobs:
  "build_and_test":
    docker: &DOCKERIMAGE
      - image: jenkinsrise/cci-v2-components:0.0.4 
    steps:
      - checkout
      - run: npm install -g gulp
      - run: npm install
      - run: npm test
      - run: npm build

workflows:
  version: 2
  workflow1:
    jobs:
      - "build_and_test"
